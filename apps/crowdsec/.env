# Generate a key using the following command:
# echo "$(openssl rand -base64 64 | tr -d '=/' | tr -d '\n')"
CROWDSEC_LAPI_KEY=CHANGEME

# CAPI (community blocklist ~21k IPs) enrolls automatically on first boot — no manual steps needed.
# Console enrollment at app.crowdsec.net is a premium feature and not required.